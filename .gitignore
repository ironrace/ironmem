/target
*.swp
*.swo
.DS_Store
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal

# Development artifacts (Codex/Claude task files)
CODEX_TASK_*.md
docs/collab_*.md

# Superpowers working artifacts (writing-plans output, /collab bridges,
# design specs). Per-session and developer-facing, not durable repo content.
# The canonical protocol/architecture lives in docs/COLLAB.md and the
# in-tree Rust source. Recovery from git history if any individual file
# is needed later.
docs/superpowers/

# Local tool and editor state (personal, non-portable)
.cargo/
.claude/
.playwright-mcp/
__pycache__/
*.pyc

benchmarks/provbench/work/
benchmarks/provbench/labeler/target/
benchmarks/provbench/baseline/target/
benchmarks/provbench/scoring/target/
benchmarks/provbench/phase1/target/
