/target
*.swp
*.swo
.DS_Store
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal

# Development artifacts (Codex/Claude task files)
CODEX_TASK_*.md
docs/collab_*.md

# Superpowers plan + scratch artifacts (writing-plans output, /collab bridges).
# These are per-session working artifacts, not durable spec; the canonical
# protocol/architecture lives in docs/COLLAB.md and similar. Specs under
# docs/superpowers/specs/ are tracked deliberately.
docs/superpowers/plans/

# Local tool and editor state (personal, non-portable)
.cargo/
.claude/
.playwright-mcp/
__pycache__/
*.pyc

benchmarks/provbench/work/
benchmarks/provbench/labeler/target/
benchmarks/provbench/baseline/target/
benchmarks/provbench/scoring/target/
benchmarks/provbench/phase1/target/
